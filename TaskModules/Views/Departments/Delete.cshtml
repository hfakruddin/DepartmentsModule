@model TaskModules.Entities.Models.Department
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>Delete</h1>
        @{        
            if (ViewBag.IsRootDept == true)
            {
                <h3 class="bg-danger">@Model.DepartmentName is a Root department. <br/> It can't be deleted before sub departments.</h3>
                <a asp-action="Index">Back to List</a>
            }
            else if (ViewBag.IsParentDept == true)
            {
                <h3 class="bg-danger">@Model.DepartmentName is a parent department. <br/> It can't be deleted before sub departments.</h3>
                <a asp-action="Index">Back to List</a>
            }
            else
            {
                <h3>Are you sure you want to delete this?</h3>
                <div>
                    <h4>Department</h4>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-2">
                            Department Name @* @Html.DisplayNameFor(model => model.DepartmentName) *@
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(model => model.DepartmentName)
                        </dd>
                        <dt class="col-sm-2">
                            Parent Department
                            @* @Html.DisplayNameFor(Model => Model.ParentDept) *@
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(Model => Model.ParentDept.DepartmentName)
                        </dd>
                    </dl>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="DepartmentId" />
                        <input type="submit" value="Delete" class="btn btn-danger" /> |
                        <a asp-action="Index">Back to List</a>
                    </form>
                </div>

            }
        }

 @*    }
    else if (ViewBag.DeleteAllRequest == true)
    {
        // <h1>Delete All</h1>
        // <h3 class="bg-danger">You have selected to Delete All Departments. <br/>
        // Are you sure you want to delete this? Once deleted you cannot retrieve them back..</h3>
        // <form asp-action="DeleteAll">
        //     <input type="hidden" id="CDel" value="Delpage" />
        //     <input type="submit" value="Delete All Departments" class="btn btn-danger" /> |
        //     <a asp-action="Index">Back to List</a>
        // </form>
    }
}
 *@